Need to install this package first for MOGWO and MOPSO
<!pip install torch pandas numpy scikit-learn scipy matplotlib>

Need to install this package for NSGA_iii
<!pip install pymoo>

Need to install these packages for MOEAD
<!pip install --upgrade --force-reinstall sympy>
<!pip install --upgrade --force-reinstall matplotlib scipy>
